# CLAUDE.md — 音の欠片さがし コンサートゲーム 要件定義

このリポジトリは「音の欠片(おとのかけら)」を探して集め、コンサートを開く探索型ゲームのプロジェクトです。
Claude Code で開発する際は、必ず本ドキュメントの要件に従ってください。

## 1. ゲームコンセプト

- プレイヤーはキャラクター(`assets/character_player.png` のうさ耳ヘッドホンの男の子)を操作し、マップを探索する。
- マップ内に散らばった **5色の「音の欠片」(赤・青・黄色・緑・白)** をすべて集めることが目標。
- 全色の欠片が集まるとコンサートが開かれ、エンディングへ進む。
- エンディングでは **曲(`assets/concert_song.mp3`)が再生され、イラスト(`assets/ending_illustration.png`)が表示される**。

## 2. ゲームフロー

1. **タイトル画面**: ゲームタイトルとスタートボタンを表示。タップ/クリックで探索開始。
2. **探索パート**: マップ上を移動しながら音の欠片を探す。
3. **収集判定**: プレイヤーが進んだ先のマスに欠片があれば、その欠片を拾う。
4. **クリア条件**: 赤・青・黄色・緑・白の 5 色すべての欠片を集める。
5. **エンディング(コンサート)**: 全色収集後、コンサート演出へ移行。
   - `assets/concert_song.mp3` を再生する。
   - `assets/ending_illustration.png`(青い翼のコンサート衣装の少年)を表示する。

## 3. 操作方法(タップ操作)

画面上に **タップキーを 2 つ** 配置する。スマートフォンでのタップ操作を前提とし、PC ではクリックでも操作可能にする。

| ボタン | 役割 |
|--------|------|
| **方向変更タップ** | タップするたびにキャラクターの向きを変更する(例: 上→右→下→左の順に回転) |
| **進行タップ** | 現在向いている方向へ 1 マス進む。進んだ先に欠片があれば自動的に拾う |

- 操作はこの 2 ボタンのみで完結させる(キーボード操作は補助として任意対応)。
- ボタンはスマホでも押しやすいサイズ・配置(画面下部など)にする。
- 現在の向きが一目で分かる表示(キャラクターの向き、または矢印表示)を行う。

## 4. 音の欠片の仕様

- 欠片は **赤・青・黄色・緑・白** の 5 色。各色 1 個以上をマップに配置する。
- 欠片は色が判別できる見た目(音符やクリスタル形の欠片など)で描画する。
- 拾った欠片は画面上の **収集状況 UI** に反映する(5 色分のスロットを常時表示し、取得済みの色を点灯させる)。
- 欠片を拾ったときは効果音・エフェクトなどのフィードバックを入れる(任意だが推奨)。

## 5. 画面構成

| 画面 | 内容 |
|------|------|
| タイトル画面 | タイトルロゴ、スタートボタン |
| 探索画面 | マップ、プレイヤーキャラクター、欠片、収集状況 UI、方向変更/進行の 2 ボタン |
| エンディング画面 | エンディングイラスト表示、楽曲再生、クリアメッセージ(「コンサートが開かれた」等)、タイトルへ戻るボタン |

## 6. 使用アセット

| ファイル | 用途 |
|----------|------|
| `assets/character_player.png` | 探索パートのプレイヤーキャラクター(青髪・うさ耳ヘッドホンのちびキャラ) |
| `assets/ending_illustration.png` | エンディング(コンサート)で表示するイラスト(青い翼・ヘッドホンの少年) |
| `assets/concert_song.mp3` | エンディングで再生する楽曲 |

- アセットは追加せず、上記の既存アセットを使うこと(欠片や UI は CSS / Canvas 描画で作成してよい)。
- 画像は縦横比を保って表示し、モバイル画面でもはみ出さないようにする。

## 7. 技術要件

- **HTML / CSS / JavaScript のみで動作する静的 Web ゲーム**として実装する(ビルド不要、`index.html` をブラウザで開けば遊べること)。
- 外部 CDN や外部ライブラリに依存しない(オフラインでも動作すること)。
- スマートフォン縦画面を基準にしたレスポンシブ対応。タップ操作(`touchstart` / `click`)に対応する。
- 音声再生はブラウザの自動再生制限を考慮し、ユーザー操作(タップ)を起点に再生する。
- マップはグリッド(マス目)ベースとし、壁や障害物を配置して探索の楽しさを持たせる。

## 8. ディレクトリ構成(想定)

```
game1/
├── CLAUDE.md            # 本ドキュメント(要件定義)
├── index.html           # ゲーム本体のエントリポイント
├── css/
│   └── style.css
├── js/
│   └── game.js
└── assets/
    ├── character_player.png
    ├── ending_illustration.png
    └── concert_song.mp3
```

## 9. 開発ルール

- 開発ブランチ: `claude/sound-fragment-game-requirements-luc13m`
- コミットメッセージは変更内容が分かる日本語または英語で記述する。
- 実装時は本要件を満たしているか、以下のチェックリストで確認すること。

### 完成チェックリスト

- [ ] タイトル画面からゲームを開始できる
- [ ] 方向変更タップでキャラクターの向きが変わる
- [ ] 進行タップで向いている方向に 1 マス進む
- [ ] 進んだ先に欠片があれば拾い、収集状況 UI に反映される
- [ ] 赤・青・黄色・緑・白の 5 色すべてを集めるとエンディングに移行する
- [ ] エンディングで `concert_song.mp3` が再生される
- [ ] エンディングで `ending_illustration.png` が表示される
- [ ] スマートフォン(縦画面)でタップ操作のみでクリアできる
